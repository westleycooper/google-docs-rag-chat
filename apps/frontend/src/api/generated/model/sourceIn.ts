/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * RAGDrive API
 * 
RAG chat over Google Drive and other document sources.

**Retrieval** is hybrid: dense pgvector search and Postgres full-text search are
fused with Reciprocal Rank Fusion, then reranked (ADR-0004). Every turn streams
its retrieval trace so a wrong answer can be diagnosed rather than merely
noticed (ADR-0009).

**Permissions** are respected during ingestion. A folder that cannot be read is
skipped *with an audit record* and never fails the run, because a silent skip is
indistinguishable from an empty folder (ADR-0003).

 * OpenAPI spec version: 0.1.0
 */
import type { SourceInAuthMode } from './sourceInAuthMode';
import type { SourceInMaxDocumentBytes } from './sourceInMaxDocumentBytes';

export interface SourceIn {
  auth_mode: SourceInAuthMode;
  /**
   * Reference into the KMS-backed credential store. Never the credential itself.
   * @minLength 1
   */
  credential_ref: string;
  enabled?: boolean;
  exclude_mime_types?: string[];
  include_mime_types?: string[];
  max_document_bytes?: SourceInMaxDocumentBytes;
  /**
   * @minLength 1
   * @maxLength 200
   */
  name: string;
  /**
   * The effective identity ingestion acts as. Defines the corpus boundary and appears on every skip record.
   * @minLength 1
   */
  principal: string;
  provider?: string;
  root_folder_ids?: string[];
}
