/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Ragoogle API
 * 
RAG chat over Google Drive and other document sources.

**Retrieval** is hybrid: dense pgvector search and Postgres full-text search are
fused with Reciprocal Rank Fusion, then reranked (ADR-0004). Every turn streams
its retrieval trace so a wrong answer can be diagnosed rather than merely
noticed (ADR-0009).

**Permissions** are respected during ingestion. A folder that cannot be read is
skipped *with an audit record* and never fails the run, because a silent skip is
indistinguishable from an empty folder (ADR-0003).

 * OpenAPI spec version: 0.1.0
 */
import type { CaseOutExpectedAnswer } from './caseOutExpectedAnswer';
import type { CaseOutNotes } from './caseOutNotes';
import type { CaseOutSourceTurnId } from './caseOutSourceTurnId';

export interface CaseOut {
  case_id: string;
  expected_answer?: CaseOutExpectedAnswer;
  /** Chunks a correct answer should have retrieved. Supplying these is what makes retrieval scorable independently of generation, and a regression attributable to a stage. */
  expected_chunk_ids?: string[];
  notes?: CaseOutNotes;
  /**
   * @minLength 1
   * @maxLength 8000
   */
  question: string;
  scores_generation: boolean;
  scores_retrieval: boolean;
  /** Set when promoting a real turn, so datasets stay grounded in answers users actually got wrong. */
  source_turn_id?: CaseOutSourceTurnId;
  tags?: string[];
}
